# Packages Custom Helper

This folder contains the repository manifest helper used by `git-repository-update`.

- Helper version: `0.1.0`
- Included for: `git_repository_update`

The app imports this local copy so it does not require a sibling checkout, Git submodule, or package install.
